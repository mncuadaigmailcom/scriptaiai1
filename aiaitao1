-- Chờ game load
repeat task.wait() until game:IsLoaded()

local ScreenGui = Instance.new("ScreenGui")
local ImageButton = Instance.new("ImageButton")
local UICorner = Instance.new("UICorner")

ScreenGui.Parent = game.CoreGui
ScreenGui.ZIndexBehavior = Enum.ZIndexBehavior.Sibling

ImageButton.Parent = ScreenGui
ImageButton.BackgroundColor3 = Color3.fromRGB(0, 0, 0)
ImageButton.BorderSizePixel = 0
ImageButton.Position = UDim2.new(0.10615778, 0, 0.16217947, 0)
ImageButton.Size = UDim2.new(0, 40, 0, 40)
ImageButton.Draggable = true
ImageButton.Image = "rbxassetid://101299344665163"

UICorner.CornerRadius = UDim.new(1, 10)
UICorner.Parent = ImageButton

-- Tải Fluent UI Library
local success, Fluent = pcall(function()
    return loadstring(game:HttpGet("https://raw.githubusercontent.com/dawid-scripts/Fluent/master/Module.lua"))()
end)

if not success then
    -- Thử link dự phòng
    success, Fluent = pcall(function()
        return loadstring(game:HttpGet("https://raw.githubusercontent.com/dawid-scripts/Fluent/main/Main.lua"))()
    end)
end

if not success then
    warn("Không thể tải Fluent UI Library!")
    return
end

-- Tạo cửa sổ chính
local Window = Fluent:CreateWindow({
    Title = "Banana Hub USB",
    SubTitle = "by quocdungskibidi",
    TabWidth = 160,
    Size = UDim2.fromOffset(530, 350),
    Acrylic = true,
    Theme = "Yellow",
    MinimizeKey = Enum.KeyCode.RightControl
})

-- Biến kiểm soát ẩn/hiện
local isVisible = true

-- Nút toggle
ImageButton.MouseButton1Down:Connect(function()
    isVisible = not isVisible
    if isVisible then
        Window:Show()
    else
        Window:Hide()
    end
end)

-- Tạo Tabs
local Tabs = {
    Main = Window:AddTab({ Title = "Main", Icon = "home" }),
    Scripts = Window:AddTab({ Title = "Scripts", Icon = "code" }),
    Teleport = Window:AddTab({ Title = "Teleport", Icon = "map-pin" }),
    Misc = Window:AddTab({ Title = "Misc", Icon = "settings" })
}

-- Tab Main
Tabs.Main:AddButton({
    Title = "Discord Server",
    Description = "Tham gia Discord của chúng tôi",
    Callback = function()
        setclipboard("https://discord.gg/chuoihub")
        Fluent:Notify({
            Title = "Thành công!",
            Content = "Đã sao chép link Discord",
            Duration = 3
        })
    end
})

-- Tab Scripts
local ScriptSection = Tabs.Scripts:AddSection("Script Blox Fruits")

ScriptSection:AddButton({
    Title = "Banana Hub v1",
    Description = "Script banana hub free",
    Callback = function()
        loadstring(game:HttpGet("https://raw.githubusercontent.com/kimprobloxdz/Banana-Free/refs/heads/main/Protected_5609200582002947.lua.txt"))()
    end
})

ScriptSection:AddButton({
    Title = "Redz Hub",
    Description = "Script Redz Hub",
    Callback = function()
        loadstring(game:HttpGet("https://raw.githubusercontent.com/REDzHUB/RedzLibV5/main/RedzLibV5.lua"))()
    end
})

ScriptSection:AddButton({
    Title = "W-azure",
    Description = "Script W-azure",
    Callback = function()
        loadstring(game:HttpGet("https://api.luarmor.net/files/v3/loaders/3b2169cf53bc6104dabe8e19562e5cc2.lua"))()
    end
})

ScriptSection:AddButton({
    Title = "Xero Hub",
    Description = "Script Xero Hub",
    Callback = function()
        getgenv().Team = "Pirates"
        loadstring(game:HttpGet("https://raw.githubusercontent.com/XeroHub/XeroHub/main/XeroHub.lua"))()
    end
})

ScriptSection:AddButton({
    Title = "Hoho Hub",
    Description = "Script Hoho Hub",
    Callback = function()
        loadstring(game:HttpGet("https://raw.githubusercontent.com/acsu123/HOHO_H/main/Loading_UI"))()
    end
})

ScriptSection:AddButton({
    Title = "Min Gaming",
    Description = "Script Min Gaming Hub",
    Callback = function()
        loadstring(game:HttpGet("https://raw.githubusercontent.com/Min-Min-Official/Min/main/Min.lua"))()
    end
})

-- Tab Teleport
local TeleportSection = Tabs.Teleport:AddSection("Server Hop")

TeleportSection:AddButton({
    Title = "Hop Server",
    Description = "Nhảy server khác",
    Callback = function()
        local Http = game:GetService("HttpService")
        local TPS = game:GetService("TeleportService")
        local Api = "https://games.roblox.com/v1/games/"
        
        local _place = game.PlaceId
        local _servers = Api.._place.."/servers/Public?sortOrder=Asc&limit=100"
        
        local function Hop()
            local List = Http:JSONDecode(game:HttpGet(_servers))
            if #List.data > 0 then
                TPS:TeleportToPlaceInstance(_place, List.data[math.random(1, #List.data)].id, game.Players.LocalPlayer)
            else
                Fluent:Notify({
                    Title = "Lỗi!",
                    Content = "Không tìm thấy server trống",
                    Duration = 3
                })
            end
        end
        
        Hop()
    end
})

-- Tab Misc
Tabs.Misc:AddButton({
    Title = "Rejoin Server",
    Description = "Vào lại server hiện tại",
    Callback = function()
        local ts = game:GetService("TeleportService")
        ts:Teleport(game.PlaceId, game.Players.LocalPlayer)
    end
})

Tabs.Misc:AddButton({
    Title = "Hide/Show GUI",
    Description = "Ẩn/Hiện toàn bộ giao diện",
    Callback = function()
        ScreenGui.Enabled = not ScreenGui.Enabled
    end
})

-- Thông báo load thành công
Fluent:Notify({
    Title = "Banana Hub USB",
    Content = "Script đã load thành công!",
    Duration = 5
})

print("Banana Hub USB đã load xong!")
