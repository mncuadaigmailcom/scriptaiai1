--[[
    FLY TO PLAYER & SAVED LOCATIONS (EXPLICIT BUTTONS VERSION)
    - THEME: WHITE-GREY TRANSLUCENT (SỌC XEN KẼ TRẮNG XÁM TRONG SUỐT)
    - NÚT BAY TÁCH BIỆT RÕ RÀNG CHO NGƯỜI CHƠI VÀ TỌA ĐỘ LƯU
    - NÚT "🚀 BAY NGAY" TRỰC TIẾP TẠI MỖI TỌA ĐỘ
    - CHO PHÉP ĐẶT TÊN CHO TỌA ĐỘ KHI LƯU
    - CƠ CHẾ BAY 3 BƯỚC AN TOÀN (LÊN CAO -> SANG NGANG -> HẠ CÁNH)
--]]

local TweenService = game:GetService("TweenService")
local Players = game:GetService("Players")
local LocalPlayer = Players.LocalPlayer
local RunService = game:GetService("RunService")
local UserInputService = game:GetService("UserInputService")
local CoreGui = game:GetService("CoreGui")

-- Variables
local selectedPlayer = nil
local selectedLocation = nil
local savedLocations = {} -- { {name = "...", pos = Vector3} }

local isFlying = false
local stopRequested = false
local currentTween = nil
local noclipConnection = nil

-- Visual Height Pillar Variables
local heightVisualPart = nil
local heightVisualConn = nil
local showHeightVisual = false

-- Color Theme (White - Grey Translucent Zebra)
local COLOR_MAIN_BG = Color3.fromRGB(30, 33, 40)
local COLOR_TITLE_BG = Color3.fromRGB(50, 55, 65)
local COLOR_STRIPE_EVEN = Color3.fromRGB(240, 242, 245) -- Trắng xám sáng
local COLOR_STRIPE_ODD = Color3.fromRGB(180, 185, 195)  -- Xám vừa
local WATER_BLUE = Color3.fromRGB(0, 140, 240)           -- Chọn người
local COLOR_GREEN = Color3.fromRGB(45, 160, 85)          -- Chọn tọa độ
local COLOR_DEAD = Color3.fromRGB(220, 40, 40)           -- Màu đỏ khi gục
local TEXT_DARK = Color3.fromRGB(20, 20, 25)
local TEXT_LIGHT = Color3.fromRGB(255, 255, 255)

-- UI Construction
local ScreenGui = Instance.new("ScreenGui")
ScreenGui.Name = "FlyToPlayerGlassExplicit"
ScreenGui.Parent = (RunService:IsStudio() and LocalPlayer.PlayerGui) or CoreGui
ScreenGui.ResetOnSpawn = false

-- Main Frame
local MainFrame = Instance.new("Frame")
MainFrame.Name = "MainFrame"
MainFrame.Size = UDim2.new(0, 360, 0, 560)
MainFrame.Position = UDim2.new(0.5, -180, 0.5, -280)
MainFrame.BackgroundColor3 = COLOR_MAIN_BG
MainFrame.BackgroundTransparency = 0.2
MainFrame.BorderColor3 = Color3.fromRGB(220, 225, 235)
MainFrame.BorderSizePixel = 1
MainFrame.Active = true
MainFrame.Draggable = true
MainFrame.Parent = ScreenGui

local MainCorner = Instance.new("UICorner")
MainCorner.CornerRadius = UDim.new(0, 8)
MainCorner.Parent = MainFrame

-- Title Bar
local Title = Instance.new("TextLabel")
Title.Size = UDim2.new(1, -60, 0, 38)
Title.Position = UDim2.new(0, 0, 0, 0)
Title.BackgroundColor3 = COLOR_TITLE_BG
Title.BackgroundTransparency = 0.2
Title.Text = "  ✈️ FLY TO PLAYER & POSITIONS"
Title.TextColor3 = TEXT_LIGHT
Title.TextSize = 14
Title.Font = Enum.Font.SourceSansBold
Title.TextXAlignment = Enum.TextXAlignment.Left
Title.Parent = MainFrame

local TitleCorner = Instance.new("UICorner")
TitleCorner.CornerRadius = UDim.new(0, 8)
TitleCorner.Parent = Title

-- Minimize Button (-)
local MinimizeBtn = Instance.new("TextButton")
MinimizeBtn.Size = UDim2.new(0, 28, 0, 28)
MinimizeBtn.Position = UDim2.new(1, -56, 0, 5)
MinimizeBtn.BackgroundColor3 = Color3.fromRGB(200, 200, 210)
MinimizeBtn.BackgroundTransparency = 0.3
MinimizeBtn.Text = "-"
MinimizeBtn.TextColor3 = TEXT_DARK
MinimizeBtn.Font = Enum.Font.SourceSansBold
MinimizeBtn.TextSize = 18
MinimizeBtn.Parent = MainFrame

-- Close Button (X)
local CloseBtn = Instance.new("TextButton")
CloseBtn.Size = UDim2.new(0, 28, 0, 28)
CloseBtn.Position = UDim2.new(1, -32, 0, 5)
CloseBtn.BackgroundColor3 = Color3.fromRGB(230, 60, 60)
CloseBtn.BackgroundTransparency = 0.2
CloseBtn.Text = "X"
CloseBtn.TextColor3 = TEXT_LIGHT
CloseBtn.Font = Enum.Font.SourceSansBold
CloseBtn.TextSize = 14
CloseBtn.Parent = MainFrame

-- Open Button
local OpenBtn = Instance.new("TextButton")
OpenBtn.Size = UDim2.new(0, 110, 0, 32)
OpenBtn.Position = UDim2.new(0.01, 0, 0.4, 0)
OpenBtn.BackgroundColor3 = COLOR_MAIN_BG
OpenBtn.BackgroundTransparency = 0.2
OpenBtn.BorderColor3 = Color3.fromRGB(255, 255, 255)
OpenBtn.Text = "📖 Mở Menu Fly"
OpenBtn.TextColor3 = TEXT_LIGHT
OpenBtn.Font = Enum.Font.SourceSansBold
OpenBtn.TextSize = 13
OpenBtn.Visible = false
OpenBtn.Draggable = true
OpenBtn.Parent = ScreenGui

---------------------------------------------------------
-- SECTION 1: DANH SÁCH NGƯỜI CHƠI
---------------------------------------------------------
local SearchBox = Instance.new("TextBox")
SearchBox.Size = UDim2.new(0.62, 0, 0, 26)
SearchBox.Position = UDim2.new(0.04, 0, 0, 44)
SearchBox.BackgroundColor3 = Color3.fromRGB(240, 240, 245)
SearchBox.BackgroundTransparency = 0.2
SearchBox.PlaceholderText = "🔍 Tìm tên..."
SearchBox.PlaceholderColor3 = Color3.fromRGB(100, 100, 110)
SearchBox.Text = ""
SearchBox.TextColor3 = TEXT_DARK
SearchBox.Font = Enum.Font.SourceSans
SearchBox.TextSize = 13
SearchBox.Parent = MainFrame

local RefreshBtn = Instance.new("TextButton")
RefreshBtn.Size = UDim2.new(0.28, 0, 0, 26)
RefreshBtn.Position = UDim2.new(0.68, 0, 0, 44)
RefreshBtn.BackgroundColor3 = Color3.fromRGB(220, 225, 235)
RefreshBtn.BackgroundTransparency = 0.2
RefreshBtn.Text = "🔄 Tải Lại"
RefreshBtn.TextColor3 = TEXT_DARK
RefreshBtn.Font = Enum.Font.SourceSansBold
RefreshBtn.TextSize = 12
RefreshBtn.Parent = MainFrame

local PlayerList = Instance.new("ScrollingFrame")
PlayerList.Size = UDim2.new(0.92, 0, 0, 110)
PlayerList.Position = UDim2.new(0.04, 0, 0, 74)
PlayerList.BackgroundColor3 = Color3.fromRGB(15, 15, 20)
PlayerList.BackgroundTransparency = 0.5
PlayerList.BorderColor3 = Color3.fromRGB(200, 200, 210)
PlayerList.ScrollBarThickness = 5
PlayerList.Parent = MainFrame

local UIListLayout1 = Instance.new("UIListLayout")
UIListLayout1.Parent = PlayerList
UIListLayout1.SortOrder = Enum.SortOrder.LayoutOrder

---------------------------------------------------------
-- SECTION 2: QUẢN LÝ TỌA ĐỘ ĐÃ LƯU
---------------------------------------------------------
local PosNameBox = Instance.new("TextBox")
PosNameBox.Size = UDim2.new(0.58, 0, 0, 26)
PosNameBox.Position = UDim2.new(0.04, 0, 0, 190)
PosNameBox.BackgroundColor3 = Color3.fromRGB(240, 240, 245)
PosNameBox.BackgroundTransparency = 0.2
PosNameBox.PlaceholderText = "Tên tọa độ (Ví dụ: Nhà, Rương...)"
PosNameBox.PlaceholderColor3 = Color3.fromRGB(100, 100, 110)
PosNameBox.Text = ""
PosNameBox.TextColor3 = TEXT_DARK
PosNameBox.Font = Enum.Font.SourceSans
PosNameBox.TextSize = 12
PosNameBox.Parent = MainFrame

local SavePosBtn = Instance.new("TextButton")
SavePosBtn.Size = UDim2.new(0.32, 0, 0, 26)
SavePosBtn.Position = UDim2.new(0.64, 0, 0, 190)
SavePosBtn.BackgroundColor3 = COLOR_GREEN
SavePosBtn.BackgroundTransparency = 0.1
SavePosBtn.Text = "📌 LƯU VỊ TRÍ"
SavePosBtn.TextColor3 = TEXT_LIGHT
SavePosBtn.Font = Enum.Font.SourceSansBold
SavePosBtn.TextSize = 12
SavePosBtn.Parent = MainFrame

local SavedList = Instance.new("ScrollingFrame")
SavedList.Size = UDim2.new(0.92, 0, 0, 110)
SavedList.Position = UDim2.new(0.04, 0, 0, 222)
SavedList.BackgroundColor3 = Color3.fromRGB(15, 15, 20)
SavedList.BackgroundTransparency = 0.5
SavedList.BorderColor3 = Color3.fromRGB(200, 200, 210)
SavedList.ScrollBarThickness = 5
SavedList.Parent = MainFrame

local UIListLayout2 = Instance.new("UIListLayout")
UIListLayout2.Parent = SavedList
UIListLayout2.SortOrder = Enum.SortOrder.LayoutOrder

---------------------------------------------------------
-- SECTION 3: CÀI ĐẶT THÔNG SỐ BAY (HEIGHT & SPEED)
---------------------------------------------------------
local HeightLabel = Instance.new("TextLabel")
HeightLabel.Size = UDim2.new(0, 120, 0, 24)
HeightLabel.Position = UDim2.new(0.04, 0, 0, 340)
HeightLabel.BackgroundTransparency = 1
HeightLabel.Text = "Chiều cao (Height):"
HeightLabel.TextColor3 = TEXT_LIGHT
HeightLabel.TextXAlignment = Enum.TextXAlignment.Left
HeightLabel.Font = Enum.Font.SourceSans
HeightLabel.TextSize = 14
HeightLabel.Parent = MainFrame

local HeightBox = Instance.new("TextBox")
HeightBox.Size = UDim2.new(0.28, 0, 0, 24)
HeightBox.Position = UDim2.new(0.40, 0, 0, 340)
HeightBox.BackgroundColor3 = Color3.fromRGB(240, 240, 245)
HeightBox.BackgroundTransparency = 0.2
HeightBox.Text = "150"
HeightBox.TextColor3 = TEXT_DARK
HeightBox.Font = Enum.Font.SourceSansBold
HeightBox.TextSize = 14
HeightBox.Parent = MainFrame

local HeightVisualBtn = Instance.new("TextButton")
HeightVisualBtn.Size = UDim2.new(0.24, 0, 0, 24)
HeightVisualBtn.Position = UDim2.new(0.72, 0, 0, 340)
HeightVisualBtn.BackgroundColor3 = Color3.fromRGB(200, 200, 210)
HeightVisualBtn.BackgroundTransparency = 0.2
HeightVisualBtn.Text = "Trụ: OFF"
HeightVisualBtn.TextColor3 = TEXT_DARK
HeightVisualBtn.Font = Enum.Font.SourceSansBold
HeightVisualBtn.TextSize = 12
HeightVisualBtn.Parent = MainFrame

local SpeedLabel = Instance.new("TextLabel")
SpeedLabel.Size = UDim2.new(0, 120, 0, 24)
SpeedLabel.Position = UDim2.new(0.04, 0, 0, 370)
SpeedLabel.BackgroundTransparency = 1
SpeedLabel.Text = "Tốc độ (Speed):"
SpeedLabel.TextColor3 = TEXT_LIGHT
SpeedLabel.TextXAlignment = Enum.TextXAlignment.Left
SpeedLabel.Font = Enum.Font.SourceSans
SpeedLabel.TextSize = 14
SpeedLabel.Parent = MainFrame

local SpeedBox = Instance.new("TextBox")
SpeedBox.Size = UDim2.new(0.56, 0, 0, 24)
SpeedBox.Position = UDim2.new(0.40, 0, 0, 370)
SpeedBox.BackgroundColor3 = Color3.fromRGB(240, 240, 245)
SpeedBox.BackgroundTransparency = 0.2
SpeedBox.Text = "100"
SpeedBox.TextColor3 = TEXT_DARK
SpeedBox.Font = Enum.Font.SourceSansBold
SpeedBox.TextSize = 14
SpeedBox.Parent = MainFrame

---------------------------------------------------------
-- SECTION 4: BẢNG TRẠNG THÁI & NÚT BẮT ĐẦU BAY CHÍNH
---------------------------------------------------------
local StatusFrame = Instance.new("Frame")
StatusFrame.Size = UDim2.new(0.92, 0, 0, 36)
StatusFrame.Position = UDim2.new(0.04, 0, 0, 405)
StatusFrame.BackgroundColor3 = Color3.fromRGB(20, 25, 30)
StatusFrame.BackgroundTransparency = 0.4
StatusFrame.BorderColor3 = Color3.fromRGB(150, 155, 165)
StatusFrame.Parent = MainFrame

local StatusLabel = Instance.new("TextLabel")
StatusLabel.Size = UDim2.new(1, -10, 1, 0)
StatusLabel.Position = UDim2.new(0, 5, 0, 0)
StatusLabel.BackgroundTransparency = 1
StatusLabel.Text = "Chưa chọn Player hoặc Tọa độ nào"
StatusLabel.TextColor3 = Color3.fromRGB(255, 230, 100)
StatusLabel.Font = Enum.Font.SourceSansBold
StatusLabel.TextSize = 13
StatusLabel.TextWrapped = true
StatusLabel.Parent = StatusFrame

-- Nút Bắt Đầu Bay Linh Hoạt
local StartBtn = Instance.new("TextButton")
StartBtn.Size = UDim2.new(0.92, 0, 0, 46)
StartBtn.Position = UDim2.new(0.04, 0, 0, 450)
StartBtn.BackgroundColor3 = Color3.fromRGB(200, 200, 200)
StartBtn.BackgroundTransparency = 0.1
StartBtn.Text = "⚠️ CHƯA CHỌN MỤC TIÊU"
StartBtn.TextColor3 = TEXT_DARK
StartBtn.Font = Enum.Font.SourceSansBold
StartBtn.TextSize = 15
StartBtn.Parent = MainFrame

local StartCorner = Instance.new("UICorner")
StartCorner.CornerRadius = UDim.new(0, 6)
StartCorner.Parent = StartBtn

---------------------------------------------------------
-- LOGIC & CÁC HÀM XỬ LÝ (CORE LOGIC)
---------------------------------------------------------
local startFly -- Khai báo trước hàm bay

-- Cập nhật giao diện Nút Bay Chính
local function updateMainStartButton()
    if isFlying then
        StartBtn.Text = "🛑 DỪNG BAY (STOP)"
        StartBtn.BackgroundColor3 = Color3.fromRGB(220, 50, 50)
        StartBtn.TextColor3 = TEXT_LIGHT
    elseif selectedPlayer then
        StartBtn.Text = "✈️ BAY TỚI PLAYER: " .. string.upper(selectedPlayer.DisplayName)
        StartBtn.BackgroundColor3 = WATER_BLUE
        StartBtn.TextColor3 = TEXT_LIGHT
    elseif selectedLocation then
        StartBtn.Text = "📍 BAY TỚI TỌA ĐỘ: " .. string.upper(selectedLocation.name)
        StartBtn.BackgroundColor3 = COLOR_GREEN
        StartBtn.TextColor3 = TEXT_LIGHT
    else
        StartBtn.Text = "⚠️ CHƯA CHỌN MỤC TIÊU"
        StartBtn.BackgroundColor3 = Color3.fromRGB(200, 200, 200)
        StartBtn.TextColor3 = TEXT_DARK
    end
end

local function getTargetPart(plr)
    if not plr or not plr.Character then return nil end
    local char = plr.Character
    return char:FindFirstChild("HumanoidRootPart")
        or char:FindFirstChild("UpperTorso")
        or char:FindFirstChild("Torso")
        or char:FindFirstChild("Head")
        or char.PrimaryPart
        or char:FindFirstChildOfClass("BasePart")
end

local function isPlayerDeadOrKnocked(plr)
    if not plr or not plr.Character then return true end
    local hum = plr.Character:FindFirstChildOfClass("Humanoid")
    local targetPart = getTargetPart(plr)
    if not targetPart then return true end
    if hum and hum.Health <= 0 then return true end
    if plr.Character:FindFirstChild("Knocked") or plr.Character:FindFirstChild("Downed") or plr.Character:FindFirstChild("Ragdoll") then
        return true
    end
    return false
end

local function clearHeightVisual()
    if heightVisualPart then heightVisualPart:Destroy() heightVisualPart = nil end
    if heightVisualConn then heightVisualConn:Disconnect() heightVisualConn = nil end
end

CloseBtn.MouseButton1Click:Connect(function()
    clearHeightVisual()
    ScreenGui:Destroy()
end)

MinimizeBtn.MouseButton1Click:Connect(function()
    MainFrame.Visible = false
    OpenBtn.Visible = true
end)

OpenBtn.MouseButton1Click:Connect(function()
    MainFrame.Visible = true
    OpenBtn.Visible = false
end)

UserInputService.InputBegan:Connect(function(input, gpe)
    if not gpe and input.KeyCode == Enum.KeyCode.RightControl then
        MainFrame.Visible = not MainFrame.Visible
        OpenBtn.Visible = not MainFrame.Visible
    end
end)

-- Render Danh Sách Người Chơi
local function updatePlayerList()
    for _, child in pairs(PlayerList:GetChildren()) do
        if child:IsA("Frame") or child:IsA("TextButton") then child:Destroy() end
    end

    local filterText = string.lower(SearchBox.Text)
    local count = 0

    for _, plr in pairs(Players:GetPlayers()) do
        if plr ~= LocalPlayer then
            local nameMatch = string.find(string.lower(plr.Name), filterText, 1, true)
            local displayMatch = string.find(string.lower(plr.DisplayName), filterText, 1, true)

            if filterText == "" or nameMatch or displayMatch then
                count = count + 1
                local isEven = (count % 2 == 0)
                local isDead = isPlayerDeadOrKnocked(plr)

                local Btn = Instance.new("TextButton")
                Btn.Size = UDim2.new(1, 0, 0, 25)
                Btn.BorderSizePixel = 0
                Btn.Font = Enum.Font.SourceSans
                Btn.TextSize = 13
                Btn.Parent = PlayerList

                if isDead then
                    Btn.Text = "🔴 [GỤC/CHẾT] " .. plr.DisplayName .. " (@" .. plr.Name .. ")"
                    Btn.TextColor3 = COLOR_DEAD
                else
                    Btn.Text = "  👤 " .. plr.DisplayName .. " (@" .. plr.Name .. ")"
                    Btn.TextColor3 = TEXT_DARK
                    Btn.TextXAlignment = Enum.TextXAlignment.Left
                end

                if selectedPlayer == plr then
                    Btn.BackgroundColor3 = WATER_BLUE
                    Btn.BackgroundTransparency = 0.1
                    Btn.TextColor3 = TEXT_LIGHT
                else
                    Btn.BackgroundColor3 = isEven and COLOR_STRIPE_EVEN or COLOR_STRIPE_ODD
                    Btn.BackgroundTransparency = 0.35
                end

                Btn.MouseButton1Click:Connect(function()
                    selectedPlayer = plr
                    selectedLocation = nil
                    StatusLabel.Text = "Đã chọn Player: " .. plr.DisplayName .. (isDead and " (ĐANG GỤC)" or "")
                    updatePlayerList()
                    updateMainStartButton()
                end)
            end
        end
    end
    PlayerList.CanvasSize = UDim2.new(0, 0, 0, count * 25)
end

-- Render Danh Sách Tọa Độ Đã Lưu (Có nút BAY NGAY)
local function updateSavedList()
    for _, child in pairs(SavedList:GetChildren()) do
        if child:IsA("Frame") or child:IsA("TextButton") then child:Destroy() end
    end

    for idx, item in ipairs(savedLocations) do
        local isEven = (idx % 2 == 0)
        
        local Container = Instance.new("Frame")
        Container.Size = UDim2.new(1, 0, 0, 26)
        Container.BackgroundColor3 = (selectedLocation == item) and COLOR_GREEN or (isEven and COLOR_STRIPE_EVEN or COLOR_STRIPE_ODD)
        Container.BackgroundTransparency = (selectedLocation == item) and 0.1 or 0.35
        Container.BorderSizePixel = 0
        Container.Parent = SavedList

        -- Tên Tọa Độ
        local SelectBtn = Instance.new("TextButton")
        SelectBtn.Size = UDim2.new(0.50, 0, 1, 0)
        SelectBtn.BackgroundTransparency = 1
        SelectBtn.Text = string.format(" 📍 %s", item.name)
        SelectBtn.TextColor3 = (selectedLocation == item) and TEXT_LIGHT or TEXT_DARK
        SelectBtn.Font = Enum.Font.SourceSansBold
        SelectBtn.TextSize = 12
        SelectBtn.TextXAlignment = Enum.TextXAlignment.Left
        SelectBtn.Parent = Container

        -- NÚT BAY NGAY TRỰC TIẾP
        local FlyDirectBtn = Instance.new("TextButton")
        FlyDirectBtn.Size = UDim2.new(0.26, 0, 0.8, 0)
        FlyDirectBtn.Position = UDim2.new(0.51, 0, 0.1, 0)
        FlyDirectBtn.BackgroundColor3 = COLOR_GREEN
        FlyDirectBtn.BackgroundTransparency = 0.1
        FlyDirectBtn.Text = "🚀 Bay Ngay"
        FlyDirectBtn.TextColor3 = TEXT_LIGHT
        FlyDirectBtn.Font = Enum.Font.SourceSansBold
        FlyDirectBtn.TextSize = 11
        FlyDirectBtn.Parent = Container

        -- NÚT XÓA TỌA ĐỘ
        local DeleteBtn = Instance.new("TextButton")
        DeleteBtn.Size = UDim2.new(0.20, -2, 0.8, 0)
        DeleteBtn.Position = UDim2.new(0.78, 0, 0.1, 0)
        DeleteBtn.BackgroundColor3 = Color3.fromRGB(220, 60, 60)
        DeleteBtn.BackgroundTransparency = 0.2
        DeleteBtn.Text = "Xóa"
        DeleteBtn.TextColor3 = TEXT_LIGHT
        DeleteBtn.Font = Enum.Font.SourceSansBold
        DeleteBtn.TextSize = 11
        DeleteBtn.Parent = Container

        SelectBtn.MouseButton1Click:Connect(function()
            selectedLocation = item
            selectedPlayer = nil
            StatusLabel.Text = "Đã chọn Tọa độ: " .. item.name
            updateSavedList()
            updatePlayerList()
            updateMainStartButton()
        end)

        -- Bấm "Bay Ngay" -> Bay luôn không cần chọn
        FlyDirectBtn.MouseButton1Click:Connect(function()
            selectedLocation = item
            selectedPlayer = nil
            updateSavedList()
            updatePlayerList()
            updateMainStartButton()
            startFly()
        end)

        DeleteBtn.MouseButton1Click:Connect(function()
            if selectedLocation == item then selectedLocation = nil end
            table.remove(savedLocations, idx)
            updateSavedList()
            updateMainStartButton()
        end)
    end
    SavedList.CanvasSize = UDim2.new(0, 0, 0, #savedLocations * 26)
end

-- Lưu Tọa Độ Hiện Tại
SavePosBtn.MouseButton1Click:Connect(function()
    local char = LocalPlayer.Character
    local hrp = char and char:FindFirstChild("HumanoidRootPart")
    if hrp then
        local customName = PosNameBox.Text
        if customName == "" or string.match(customName, "^%s*$") then
            customName = "Điểm " .. (#savedLocations + 1)
        end

        table.insert(savedLocations, {
            name = customName,
            pos = hrp.Position
        })
        PosNameBox.Text = ""
        StatusLabel.Text = "Đã lưu vị trí: " .. customName
        updateSavedList()
    else
        StatusLabel.Text = "Lỗi: Không tìm thấy nhân vật!"
    end
end)

RefreshBtn.MouseButton1Click:Connect(updatePlayerList)
SearchBox:GetPropertyChangedSignal("Text"):Connect(updatePlayerList)
Players.PlayerAdded:Connect(updatePlayerList)
Players.PlayerRemoving:Connect(updatePlayerList)
updatePlayerList()

--- Visual Pillar ---
local function updateHeightVisual()
    if not showHeightVisual then clearHeightVisual() return end

    if not heightVisualPart then
        heightVisualPart = Instance.new("Part")
        heightVisualPart.Name = "FlyHeightPillar"
        heightVisualPart.Shape = Enum.PartType.Cylinder
        heightVisualPart.Material = Enum.Material.SmoothPlastic
        heightVisualPart.Color = Color3.fromRGB(200, 200, 220)
        heightVisualPart.Transparency = 0.4
        heightVisualPart.Anchored = true
        heightVisualPart.CanCollide = false
        heightVisualPart.Parent = workspace
    end

    if heightVisualConn then heightVisualConn:Disconnect() end

    heightVisualConn = RunService.RenderStepped:Connect(function()
        local char = LocalPlayer.Character
        if char and char:FindFirstChild("HumanoidRootPart") and heightVisualPart then
            local flyHeight = tonumber(HeightBox.Text) or 150
            local hrp = char.HumanoidRootPart
            heightVisualPart.Size = Vector3.new(flyHeight, 2, 2)
            heightVisualPart.CFrame = CFrame.new(hrp.Position + Vector3.new(0, flyHeight / 2, 0)) * CFrame.Angles(0, 0, math.rad(90))
        end
    end)
end

HeightVisualBtn.MouseButton1Click:Connect(function()
    showHeightVisual = not showHeightVisual
    if showHeightVisual then
        HeightVisualBtn.Text = "Trụ: ON"
        HeightVisualBtn.BackgroundColor3 = Color3.fromRGB(255, 255, 255)
        updateHeightVisual()
    else
        HeightVisualBtn.Text = "Trụ: OFF"
        HeightVisualBtn.BackgroundColor3 = Color3.fromRGB(200, 200, 210)
        clearHeightVisual()
    end
end)

--- Safe Physics Reset ---
local function resetCharacterPhysics()
    if noclipConnection then noclipConnection:Disconnect() noclipConnection = nil end
    local char = LocalPlayer.Character
    if not char then return end

    local hrp = char:FindFirstChild("HumanoidRootPart")
    if hrp then
        hrp.AssemblyLinearVelocity = Vector3.zero
        hrp.AssemblyAngularVelocity = Vector3.zero

        local overlapParams = OverlapParams.new()
        overlapParams.FilterDescendantsInstances = {char}
        overlapParams.FilterType = Enum.RaycastFilterType.Exclude

        if #workspace:GetPartsInPart(hrp, overlapParams) > 0 then
            for i = 1, 5 do
                hrp.CFrame = hrp.CFrame + Vector3.new(0, 2, 0)
                if #workspace:GetPartsInPart(hrp, overlapParams) == 0 then break end
            end
        end
    end

    for _, part in pairs(char:GetDescendants()) do
        if part:IsA("BasePart") then
            if part.Name == "HumanoidRootPart" or part.Name == "UpperTorso" or part.Name == "LowerTorso" or part.Name == "Torso" then
                part.CanCollide = true
            end
        end
    end
end

local function setNoclip(state)
    if state then
        noclipConnection = RunService.Stepped:Connect(function()
            if LocalPlayer.Character then
                for _, part in pairs(LocalPlayer.Character:GetDescendants()) do
                    if part:IsA("BasePart") then part.CanCollide = false end
                end
            end
        end)
    else
        resetCharacterPhysics()
    end
end

local function tweenToTarget(hrp, targetCFrame, flySpeed)
    if stopRequested then return false end

    local distance = (targetCFrame.Position - hrp.Position).Magnitude
    local duration = distance / flySpeed

    currentTween = TweenService:Create(hrp, TweenInfo.new(duration, Enum.EasingStyle.Linear), {CFrame = targetCFrame})
    currentTween:Play()

    local finished = false
    local connection
    connection = currentTween.Completed:Connect(function() finished = true end)

    while not finished do
        if stopRequested then
            if currentTween then currentTween:Cancel() end
            if connection then connection:Disconnect() end
            return false
        end
        task.wait()
    end

    if connection then connection:Disconnect() end
    return true
end

--- Core Flying Routine ---
startFly = function()
    if isFlying then return end

    local getTargetPos = nil
    local targetName = ""

    if selectedPlayer then
        getTargetPos = function()
            local p = getTargetPart(selectedPlayer)
            return p and p.Position
        end
        targetName = selectedPlayer.DisplayName
    elseif selectedLocation then
        getTargetPos = function()
            return selectedLocation.pos
        end
        targetName = selectedLocation.name
    else
        StatusLabel.Text = "Lỗi: Bạn chưa chọn Người chơi hoặc Tọa độ!"
        return
    end

    if not getTargetPos() then
        StatusLabel.Text = "Lỗi: Không tìm thấy vị trí mục tiêu!"
        return
    end

    local char = LocalPlayer.Character
    local hrp = char and char:FindFirstChild("HumanoidRootPart")
    if not hrp then return end

    local flyHeight = tonumber(HeightBox.Text) or 150
    local flySpeed = tonumber(SpeedBox.Text) or 100

    isFlying = true
    stopRequested = false
    updateMainStartButton()

    setNoclip(true)
    local oldGravity = workspace.Gravity
    workspace.Gravity = 0

    -- Phase 1: Bay lên
    StatusLabel.Text = "Đang bay lên cao..."
    local skyPos = hrp.Position + Vector3.new(0, flyHeight, 0)
    local ok1 = tweenToTarget(hrp, CFrame.new(skyPos), flySpeed)

    -- Phase 2: Bay ngang
    if ok1 and not stopRequested then
        StatusLabel.Text = "Đang di chuyển tới: " .. targetName
        local dest = getTargetPos()
        if dest then
            local overhead = dest + Vector3.new(0, flyHeight, 0)
            local ok2 = tweenToTarget(hrp, CFrame.new(overhead), flySpeed)

            -- Phase 3: Hạ cánh
            if ok2 and not stopRequested then
                StatusLabel.Text = "Đang hạ cánh..."
                dest = getTargetPos()
                if dest then
                    local landPos = dest + Vector3.new(0, 3, 0)
                    tweenToTarget(hrp, CFrame.new(landPos), flySpeed)
                end
            end
        end
    end

    workspace.Gravity = oldGravity
    resetCharacterPhysics()
    isFlying = false
    updateMainStartButton()

    if stopRequested then
        StatusLabel.Text = "Đã dừng bay an toàn!"
    else
        StatusLabel.Text = "Đã đến nơi: " .. targetName
    end
end

-- Xử lý nút Bắt Đầu / Dừng Bay
StartBtn.MouseButton1Click:Connect(function()
    if isFlying then
        stopRequested = true
        if currentTween then currentTween:Cancel() end
        resetCharacterPhysics()
    else
        startFly()
    end
end)
